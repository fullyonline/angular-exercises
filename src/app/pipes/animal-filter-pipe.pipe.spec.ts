import { AnimalFilterPipePipe } from './animal-filter-pipe.pipe';

describe('AnimalFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AnimalFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
